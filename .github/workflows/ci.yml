name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Create venv and install
        run: |
          python3 -m venv .venv
          . .venv/bin/activate
          pip install -e .[test]
      - name: Run tests
        run: |
          . .venv/bin/activate
          pytest -q
      - name: Validate every template
        run: |
          . .venv/bin/activate
          for d in templates/*/; do
            python -m rpt validate "$d/release-please-config.json" "$d/.release-please-manifest.json"
          done
